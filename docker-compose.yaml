services:
  gina-reader:
    image: ghcr.io/jina-ai/reader:latest
    environment:
      - PORT=8080
    restart: unless-stopped
    networks:
      - coolify

networks:
  coolify:
    external: true
